module OpenstudioStandards
  # set a version higher than the installed version of OpenStudio standards that is shipped with OpenStudio CLI
  # This allows OpenStudio CLI to see it as newer, thus using it.
  VERSION = '0.5.0-ambient'.freeze
end
