machine:
  services:
    - docker

dependencies:
  override:

test:
  override:
    - pwd
    - echo "Home from circle yml:" $HOME
    - mkdir -p $CIRCLE_TEST_REPORTS/minitest
    - >
      docker run -v $(pwd):/var/openstudio-standards nrel/openstudio /var/openstudio-standards/openstudio-standards/test/bin/docker-run.sh
      
general:
  artifacts:
    - "openstudio-standards/test/output/comparison.csv"
    - "openstudio-standards/test/output/comparison_simple.csv"
    - "openstudio-standards/test/output/comparison_total.csv"      
